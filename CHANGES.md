# สรุปการแก้ไข (23 ส.ค. 2569)

ทั้งหมดตรวจโดยรันแอปจริงใน headless browser (ไม่ใช่เดาจากโค้ดเฉยๆ) เจอ 4 จุดที่มี evidence ยืนยันชัดเจน แก้แล้วดังนี้:

## 1. โลโก้ / จุดสีทองที่โดนตัดหาย (บั๊กที่ยืนยันได้ 100%)
**ต้นเหตุ:** `.brand-mark-orbit` ใช้ `transform-box:fill-box; transform-origin:center;` ซึ่งเบราว์เซอร์คำนวณจุดหมุนผิด (หมุนรอบพิกัด 83.5,80 แทนที่จะเป็นกึ่งกลางจริง 100,100) รวมกับ SVG มี `overflow:hidden` โดย default ทำให้วงแหวน+จุดที่ควรโคจรอยู่ในโลโก้ เหวี่ยงออกนอกกรอบแล้วโดนตัดหายไปเกือบทั้งรอบ
**แก้:** เปลี่ยนเป็น `transform-box:view-box; transform-origin:100px 100px;` (บรรทัด ~113) ยืนยันแล้วว่าจุดหมุนอยู่ในตำแหน่งถูกต้องตลอดแอนิเมชัน

## 2. `overflow-x:hidden` บน `<html>` (ส่งผลต่อ scroll/sticky sidebar)
**ต้นเหตุ:** ตั้ง `overflow-x:hidden` บน `<html>` โดยไม่ได้ตั้ง `overflow-y` คู่กัน ตามสเปก CSS เบราว์เซอร์จะบังคับ `overflow-y:auto` ให้อัตโนมัติ ทำให้ `<html>` กลายเป็น scroll container ซ้อนของตัวเอง — เป็นสาเหตุที่รู้กันว่าทำให้ `position:sticky` และพฤติกรรม scroll ผิดเพี้ยนในบางเบราว์เซอร์
**แก้:** เอา `overflow-x:hidden` ออกจาก `html` ย้ายไปไว้ที่ `main` แทน (ไม่ใช่ ancestor ของ sidebar แล้ว) และเพิ่ม `overflow-y:auto` ให้ `.sidebar` กันเนื้อหาล้นเวลาหน้าจอเตี้ย

## 3. แถวงาน (task) สีเขียว/แดงค้างซ้อนกับข้อความจริง
**ต้นเหตุ:** ระบบ swipe-to-act เปิดพื้นหลังสีเขียว/แดง (`swipe-active`) ทันทีที่กดเมาส์ค้าง แต่ในบาง path (เช่นลากแนวตั้งโดยไม่ตั้งใจ) ไม่ได้ปิดกลับให้ครบ ทำให้ค้างซ้อนกับเนื้อหาแถวได้
**แก้:**
- ปิด `swipe-active` ให้ครบทุก path รวมถึง path ที่ยกเลิกเพราะลากแนวตั้ง
- เพิ่ม global safety net: คลิกที่ไหนก็ได้นอกแถวงาน จะปิดสถานะ swipe ที่ค้างอยู่ทันที

## 4. แถบเขียว/แดงใน "สรุปการเงินเดือนนี้" งงว่าคืออะไร
ไม่ใช่บั๊ก แต่เพิ่ม label ให้ชัดว่าเขียว=รายรับ แดง=รายจ่าย (สัดส่วนของเดือนนี้) กันสับสนว่าเป็นของพัง

---

**หมายเหตุ:** ปัญหา scroll เมาส์ไม่ได้บน PC — ทดสอบใน headless Chromium แล้ว wheel scroll ทำงานปกติ ไม่สามารถ reproduce อาการนี้ตรงๆ ในสภาพแวดล้อมทดสอบได้ 100% (อาจเกี่ยวกับ extension ในเบราว์เซอร์จริงของพี่ เช่น ตัวที่เห็นในสกรีนช็อต หรือเกี่ยวกับ bug #2 ที่แก้ไปแล้ว) ถ้าหลัง deploy แล้วยังเจอ ลองปิด extension ทั้งหมดแล้วลองใหม่ แล้วบอกผมได้ว่ายังเป็นอยู่ไหม จะตามต่อให้
